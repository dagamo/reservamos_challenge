import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 5,
    paddingVertical: 5,
    alignItems: 'center',
  },
  titleContainer: {
    marginBottom: 8,
  },
  imageContainer: {
    marginBottom: 8,
  },
});
