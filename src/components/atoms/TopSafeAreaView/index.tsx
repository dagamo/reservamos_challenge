import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';
import {styles} from './styles';

export const TopSafeAreaView = () => (
  <SafeAreaView style={styles.topSafeAreaView} />
);
